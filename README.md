# Auto-App - Система управления записями сервиса автомобилей

Полнофункциональное веб-приложение для управления записями сервиса автомобилей с бэкэнд на Node.js/Express и фронтенд на React.

## 🚀 Быстрый старт

### Установка

1. **Клонируйте репозиторий и установите зависимости:**

```bash
# Установка зависимостей фронтенда
npm install

# Установка зависимостей бэкэнда
cd server
npm install
cd ..
```

### Запуск приложения

**Вариант 1: Запуск обоих (фронтенд + бэкэнд) одновременно:**

```bash
npm run dev
```

**Вариант 2: Запуск отдельно (два терминала)**

Терминал 1 - Бэкэнд:
```bash
npm run server:dev
```

Терминал 2 - Фронтенд:
```bash
npm start
```

**Вариант 3: Production режим**

```bash
npm run start:both
```

После запуска приложение будет доступно по адресу: **http://localhost:3000**

## 📋 Структура проекта

```
auto-app/
├── server/                 # Node.js/Express бэкэнд
│   ├── server.js          # Основной сервер (порт 5000)
│   ├── data.json          # База данных (создается автоматически)
│   ├── package.json
│   ├── .env
│   ├── .gitignore
│   ├── README.md          # Документация бэкэнда
│   └── API_EXAMPLES.md    # Примеры API запросов
│
├── src/                    # React фронтенд
│   ├── components/        # React компоненты
│   │   ├── Card.js
│   │   ├── EditRecordModal.js
│   │   ├── RecordForm.js
│   │   ├── RecordList.js
│   │   └── ResultCreateRecord.js
│   ├── service/
│   │   └── api.js         # API клиент
│   ├── styles/            # CSS стили
│   ├── App.js
│   └── index.js
│
├── public/
│   ├── index.html
│   └── data/
│       └── Records.json   # Начальные данные
│
├── package.json           # Зависимости фронтенда
└── .env                   # Переменные окружения фронтенда
```

## 🔧 API Endpoints

### Базовый URL: `http://localhost:5000/api`

| Метод | Endpoint | Описание |
|-------|----------|---------|
| GET | `/health` | Проверка статуса сервера |
| GET | `/records` | Получить все записи |
| GET | `/records/:id` | Получить запись по ID |
| POST | `/records` | Создать новую запись |
| PUT | `/records/:id` | Обновить запись |
| DELETE | `/records/:id` | Удалить запись |

### Пример создания записи:

```bash
curl -X POST http://localhost:5000/api/records \
  -H "Content-Type: application/json" \
  -d '{
    "client": "Ivan Petrov",
    "car": "Toyota Camry 2015",
    "service": "Oil change",
    "price": 3500,
    "date": "2025-11-28",
    "status": "pending",
    "payment_amount": null,
    "comments": "Test comment",
    "cancellation_reason": null
  }'
```

Подробная документация API находится в `server/API_EXAMPLES.md`

## 📦 Технологический стек

**Фронтенд:**
- React 19.2.0
- React Router DOM 7.9.4
- CSS3

**Бэкэнд:**
- Node.js
- Express 4.18.2
- CORS 2.8.5
- Body Parser 1.20.2
- UUID 9.0.0

## 🛠️ Доступные команды

### Фронтенд

```bash
npm start          # Запуск приложения в режиме разработки
npm build          # Сборка для production
npm test           # Запуск тестов
npm run eject      # Отключение от Create React App (необратимо)
```

### Бэкэнд

```bash
npm run server     # Запуск сервера
npm run server:dev # Запуск сервера в режиме разработки с автопересчитыванием
```

### Оба сразу

```bash
npm run dev        # Запуск обоих с автопересчитыванием (разработка)
npm run start:both # Запуск обоих (production)
```

## 🗄️ Данные

База данных хранится в `server/data.json`. При первом запуске сервера файл создается автоматически с начальными данными из `public/data/Records.json`.

### Структура записи:

```json
{
  "id": 1,
  "client": "Ivan Petrov",
  "car": "Toyota Camry 2015",
  "service": "Oil change",
  "price": 3500,
  "date": "2025-10-05",
  "status": "completed",
  "payment_amount": 3500,
  "comments": "Service completed successfully",
  "cancellation_reason": null
}
```

### Допустимые статусы:
- `pending` - В ожидании
- `in_progress` - В процессе
- `completed` - Завершено
- `cancelled` - Отменено

## 🌍 Переменные окружения

### Фронтенд (.env)
```
REACT_APP_API_URL=http://localhost:5000/api
```

### Бэкэнд (server/.env)
```
PORT=5000
NODE_ENV=development
```

## 🚢 Развертывание

### Локальное развертывание

1. Убедитесь, что Node.js установлен (версия 14+)
2. Установите зависимости: `npm install`
3. Запустите приложение: `npm run dev`

### Production развертывание

1. Соберите фронтенд:
```bash
npm run build
```

2. Запустите сервер в production режиме:
```bash
cd server
NODE_ENV=production PORT=3000 npm start
```

## 📝 Документация

- **Фронтенд**: см. `README.md` (этот файл)
- **Бэкэнд**: см. `server/README.md`
- **Примеры API**: см. `server/API_EXAMPLES.md`
- **Функции**: см. `FEATURES.md`

## 🐛 Решение проблем

### Сервер не запускается на порту 5000
Измените порт в `server/.env`:
```
PORT=5001
```

И обновите URL фронтенда в `.env`:
```
REACT_APP_API_URL=http://localhost:5001/api
```

### CORS ошибки
Убедитесь, что:
- Бэкэнд запущен на `http://localhost:5000`
- Фронтенд запущен на `http://localhost:3000`
- CORS включен в `server/server.js` (включен по умолчанию)

### Данные не сохраняются
Проверьте:
- Наличие папки `server/` и прав на запись файлов
- Правильность конфигурации сервера
- Консоль сервера на предмет ошибок

## 📄 Лицензия

MIT

## 👤 Автор

Roma Zachet Project

---

**Для дополнительной информации смотрите документацию в папке `server/`**
