# 🚀 Быстрый старт Auto-App

## Этап 1: Установка (выполняется один раз)

```bash
# 1. Установить зависимости фронтенда
npm install

# 2. Перейти в папку сервера и установить его зависимости
cd server
npm install
cd ..
```

## Этап 2: Запуск приложения

### Вариант А: Одна команда (рекомендуется для разработки)

```bash
npm run dev
```

Это запустит одновременно:
- ✅ Фронтенд на http://localhost:3000
- ✅ Бэкэнд на http://localhost:5000

### Вариант B: Два терминала (для лучшего контроля)

**Терминал 1 - Бэкэнд:**
```bash
npm run server:dev
```
Сервер запустится в режиме разработки с автоперезагрузкой при изменении файлов.

**Терминал 2 - Фронтенд:**
```bash
npm start
```
React приложение откроется в браузере.

## 📍 Доступные URL после запуска

- **Приложение:** http://localhost:3000
- **API сервер:** http://localhost:5000
- **Health Check:** http://localhost:5000/api/health

## 📚 Документация

После запуска вы можете прочитать:

- **Полная документация проекта:** `README.md`
- **Документация бэкэнда:** `server/README.md`
- **Примеры API запросов:** `server/API_EXAMPLES.md`
- **Список функций:** `FEATURES.md`

## 🔧 Основные команды

```bash
# Фронтенд
npm start          # Запуск приложения
npm run build      # Сборка для production

# Бэкэнд
npm run server     # Запуск сервера (production)
npm run server:dev # Запуск сервера в режиме разработки

# Оба вместе
npm run dev        # Запуск обоих с автопересчитыванием
npm run start:both # Запуск обоих (production)
```

## ✨ Возможности приложения

✅ Просмотр списка записей  
✅ Создание новых записей  
✅ Редактирование существующих записей  
✅ Удаление записей  
✅ Фильтрация по статусам  
✅ Сохранение данных на сервере  
✅ Полная REST API  

## 🆘 Решение проблем

### Сервер не запускается
```bash
# Проверьте, что порт 5000 свободен, или измените его в server/.env
PORT=5001
```

### CORS ошибки
Убедитесь, что оба процесса запущены:
- Фронтенд на http://localhost:3000
- Бэкэнд на http://localhost:5000

### Данные не загружаются
Проверьте консоль браузера (F12) и консоль сервера на ошибки.

---

**Готово! Ваше приложение полностью функционально! 🎊**
