# 🚀 Быстрый старт Auto-App

## Этап 1: Установка (выполняется один раз)

```bash
# 1. Установить зависимости фронтенда
npm install

# 2. Перейти в папку сервера и установить его зависимости
cd server
npm install
cd ..
```

## Этап 2: Запуск приложения

### Вариант А: Одна команда (рекомендуется для разработки)

```bash
npm run dev
```

Это запустит одновременно:
- ✅ Фронтенд на http://localhost:3000
- ✅ Бэкэнд на http://localhost:5000

### Вариант B: Два терминала (для лучшего контроля)

**Терминал 1 - Бэкэнд:**
```bash
npm run server:dev
```
Сервер запустится в режиме разработки с автоперезагрузкой при изменении файлов.

**Терминал 2 - Фронтенд:**
```bash
npm start
```
React приложение откроется в браузере.

## 📍 Доступные URL после запуска

- **Приложение:** http://localhost:3000
- **API сервер:** http://localhost:5000
- **Health Check:** http://localhost:5000/api/health

## 📚 Документация

После запуска вы можете прочитать:

- **Полная документация проекта:** `README.md`
- **Документация бэкэнда:** `server/README.md`
- **Примеры API запросов:** `server/API_EXAMPLES.md`
- **Список функций:** `FEATURES.md`

## 🔧 Основные команды

```bash
# Фронтенд
npm start          # Запуск приложения
npm run build      # Сборка для production

# Бэкэнд
npm run server     # Запуск сервера (production)
npm run server:dev # Запуск сервера в режиме разработки

# Оба вместе
npm run dev        # Запуск обоих с автопересчитыванием
npm run start:both # Запуск обоих (production)
```

## ✨ Возможности приложения

✅ Просмотр списка записей  
✅ Создание новых записей  
✅ Редактирование существующих записей  
✅ Удаление записей  
✅ Фильтрация по статусам  
✅ Сохранение данных на сервере  
✅ Полная REST API  

## 🆘 Решение проблем

### Сервер не запускается
```bash
# Проверьте, что порт 5000 свободен, или измените его в server/.env
PORT=5001
```

### CORS ошибки
Убедитесь, что оба процесса запущены:
- Фронтенд на http://localhost:3000
- Бэкэнд на http://localhost:5000

### Данные не загружаются
Проверьте консоль браузера (F12) и консоль сервера на ошибки.

---

**Готово! Ваше приложение полностью функционально! 🎊**

# ⚡ Быстрый старт для Railway

## 📝 Чек-лист синхронизации

### ✅ Что уже сделано:
- [x] Обновлена конфигурация `server/.env` для Railway
- [x] Обновлен `db.js` для поддержки `DATABASE_URL`
- [x] Создана конфигурация `railway.json`
- [x] Создан файл `Procfile`
- [x] Обновлена переменная `REACT_APP_API_URL` в `.env`

---

## 🚀 Что делать дальше:

### Шаг 1️⃣: Получите учетные данные из Railway

1. Откройте **railway.app** → ваш проект
2. Нажмите на **PostgreSQL** в списке сервисов
3. Перейдите на вкладку **"Connect"**
4. Скопируйте **DATABASE_URL** (выглядит как: `postgresql://user:password@host:port/database`)

---

### Шаг 2️⃣: Обновите `.env` файлы

**Вариант A: Используя DATABASE_URL (рекомендуется)**

Отредактируйте `server/.env`:
```bash
PORT=5000
NODE_ENV=production
DATABASE_URL=postgresql://user:password@host:port/database
```

**Вариант B: Используя отдельные переменные**

Отредактируйте `server/.env`:
```bash
PORT=5000
NODE_ENV=production
DB_HOST=your-railway-host.railway.app
DB_PORT=5432
DB_NAME=railway
DB_USER=postgres
DB_PASSWORD=your_password
```

---

### Шаг 3️⃣: Обновите URL фронтенда

Отредактируйте `.env`:
```bash
REACT_APP_API_URL=https://your-app-name.railway.app/api
```

⚠️ **Замените `your-app-name` на реальное имя вашего приложения!**

---

### Шаг 4️⃣: Пушьте код в Railway

```bash
# Инициализируем Git (если еще не сделано)
git init
git add .
git commit -m "Setup Railway deployment with PostgreSQL"

# Добавляем репозиторий GitHub
git remote add origin https://github.com/YOUR_USERNAME/auto-app.git
git branch -M main
git push -u origin main
```

---

### Шаг 5️⃣: Развертывание

1. Откройте **railway.app**
2. Нажмите **"New Project"** → **"Deploy from GitHub"**
3. Выберите свой репозиторий
4. Railway **автоматически**:
   - ✅ Создаст таблицы PostgreSQL
   - ✅ Загрузит начальные данные
   - ✅ Запустит сервер

---

## 🔍 Проверка

Когда деплоймент завершится, проверьте:

```bash
curl https://your-app-name.railway.app/api/health
```

Должны получить:
```json
{
  "status": "OK",
  "message": "Server is running",
  "database": "PostgreSQL"
}
```

---

## 📊 Структура таблиц (создаются автоматически)

```
records         - Записи о работах
services        - Услуги
users           - Пользователи (операторы, мастера, клиенты)
shifts          - Смены операторов
transactions    - Платежи
master_services - Связь мастеров с услугами
shift_logs      - Логи операций
```

---

## 🐛 Если что-то не работает

1. **Проверьте логи** в Railway: вкладка **"Deployments"** → **"View Logs"**
2. **Локально протестируйте**:
   ```bash
   cd server
   npm install
   npm start
   ```
3. **Прочитайте** `RAILWAY_DEPLOYMENT.md` для подробного гайда
4. **Проверьте** что все переменные окружения установлены

---

## 📚 Дополнительно

- Полный гайд: `RAILWAY_DEPLOYMENT.md`
- API примеры: `server/API_EXAMPLES.md`
- Настройка PostgreSQL: `server/POSTGRES_SETUP.md`
- Бэкенд информация: `BACKEND_SUMMARY.md`

---

**Готовы? Начните с Шага 1! 🚀**
