# Auto Service App - Система управления записями

## 📋 Реализованные функции

### 1. ✅ Редактирование и удаление записей
- Нажмите на кнопку **✎** (редактировать) на карточке записи
- Измените необходимые данные в модальном окне
- Нажмите **Сохранить** для применения изменений
- Для удаления нажмите кнопку **🗑** (удалить) - появится подтверждение

### 2. ✅ Оформление карточек со значками статусов
Каждая карточка отображает:
- **Значок статуса** с цветовой кодировкой:
  - 🟢 **✓ Зелёный** - Проведена (Completed)
  - 🔴 **✕ Красный** - Отмена (Cancelled)
  - 🟡 **⟳ Жёлтый** - В работе (In Progress)
  - ⚫ **⋯ Серый** - Ожидание (Pending)
- Цветная левая полоса на карточке
- Стилизованный бейдж статуса в футере карточки
- Информация о клиенте, автомобиле, услуге, цене и дате

### 3. ✅ Фильтрация по статусам
Используйте кнопки фильтра на странице "Список записей":
- **Все** - показать все записи
- **✓ Проведены** - только проведённые записи (зелёные)
- **✕ Отмены** - только отменённые записи (красные)
- **⟳ В работе** - только записи в процессе выполнения (жёлтые)
- **⋯ Ожидание** - записи в ожидании (серые)

Каждая кнопка показывает количество записей в этой категории.

### 4. ✅ При отмене записи - указание причины
Когда вы меняете статус записи на "Отмена" (Cancelled):
- Появляется обязательное поле **"Причина отмены"**
- Введите детальное описание причины отмены
- Причина будет отображаться на карточке в красном блоке

### 5. ✅ При проведении записи - сумма оплаты и комментарии
Когда вы меняете статус на "Проведена" (Completed):
- Появляется обязательное поле **"Сумма оплаты (₽)"**
- Появляется опциональное поле **"Комментарии"**
- Сумма оплаты отображается на карточке в зелёном блоке
- Комментарии выводятся в виде стилизованного текста

## 🚀 Как использовать

### Создание новой записи
1. Перейдите на страницу **"➕ Создать запись"**
2. Заполните все обязательные поля:
   - Клиент (ФИО)
   - Автомобиль (марка и модель)
   - Услуга (название)
   - Цена (в рублях)
   - Дата
3. Нажмите **"Создать запись"**

### Просмотр и фильтрация записей
1. Перейдите на страницу **"📋 Список записей"**
2. Выберите нужный фильтр по статусам
3. Кликните на карточку для просмотра полной информации

### Редактирование записи
1. На странице списка найдите нужную запись
2. Нажмите кнопку **✎** (редактировать)
3. В модальном окне измените необходимые данные
4. Если меняете статус:
   - На **"Проведена"** - заполните сумму оплаты (обязательно) и комментарии (опционально)
   - На **"Отмена"** - заполните причину отмены (обязательно)
5. Нажмите **"Сохранить"**

### Удаление записи
1. На странице списка найдите нужную запись
2. Нажмите кнопку **🗑** (удалить)
3. Подтвердите удаление в диалоговом окне

## 📊 Структура данных записи

```json
{
  "id": 1,
  "client": "Ivan Petrov",
  "car": "Toyota Camry 2015",
  "service": "Oil change",
  "price": 3500,
  "date": "2025-10-05",
  "status": "completed",
  "payment_amount": 3500,
  "comments": "Service completed successfully",
  "cancellation_reason": null
}
```

## 🎨 Цвета статусов

| Статус | Цвет | Код | Иконка |
|--------|------|------|--------|
| Completed | Зелёный | #28a745 | ✓ |
| Cancelled | Красный | #dc3545 | ✕ |
| In Progress | Жёлтый | #ff9800 | ⟳ |
| Pending | Серый | #6c757d | ⋯ |

## 💾 Хранение данных

- **Исходные данные**: загружаются из `/public/data/Records.json`
- **Новые записи**: сохраняются в `localStorage` браузера
- **Изменения**: автоматически синхронизируются с `localStorage`

## 🛠 Технологический стек

- **React 19.2.0** - UI фреймворк
- **React Router 7.9.4** - навигация между страницами
- **CSS3** - стилизация с адаптивным дизайном
- **localStorage** - сохранение данных на клиенте

## 📱 Адаптивный дизайн

Приложение полностью адаптировано для мобильных устройств:
- Карточки автоматически переходят в одноколоночный формат на мобильных
- Формы оптимизированы для сенсорного ввода
- Модальные окна масштабируются под размер экрана

---

**Версия**: 1.0.0  
**Дата последнего обновления**: 28 ноября 2025

# 🎯 Как начать работу с Auto-App

## 📥 Установка (выполняется один раз)

```bash
# Перейдите в папку проекта
cd auto-app

# Установите зависимости фронтенда
npm install

# Установите зависимости бэкэнда
cd server
npm install
cd ..
```

**Готово!** Все необходимые пакеты установлены.

---

## 🚀 Запуск приложения

### 🔥 Самый простой способ (Рекомендуется)

Выполните одну команду в корне проекта:

```bash
npm run dev
```

Это запустит одновременно:
- ✅ **Фронтенд** на http://localhost:3000 (React приложение)
- ✅ **Бэкэнд** на http://localhost:5000 (Express API)

Приложение автоматически откроется в браузере!

### 💻 Альтернативно: Два отдельных терминала

Если хотите больше контроля, откройте два терминала:

**Терминал 1 - Запуск бэкэнда:**
```bash
npm run server:dev
```
Вы увидите сообщение:
```
✅ Сервер запущен на порту 5000
📍 API доступен по адресу: http://localhost:5000/api
```

**Терминал 2 - Запуск фронтенда:**
```bash
npm start
```
React приложение откроется в браузере на http://localhost:3000

---

## 🌐 После запуска

### Приложение доступно по адресам:

| URL | Назначение |
|-----|-----------|
| http://localhost:3000 | 🎨 Главное приложение (фронтенд) |
| http://localhost:5000/api/records | 📊 API со всеми записями |
| http://localhost:5000/api/health | 💚 Проверка статуса сервера |

### Что вы можете делать:

✅ **Просматривать** список всех записей  
✅ **Создавать** новые записи  
✅ **Редактировать** существующие записи  
✅ **Удалять** ненужные записи  
✅ **Фильтровать** по статусам (ожидание, в процессе, завершено, отменено)  
✅ **Все данные сохраняются** на сервере автоматически  

---

## 📚 Документация

После запуска прочитайте эту документацию для получения подробной информации:

1. **`QUICKSTART.md`** - Быстрый старт (этот файл)
2. **`README.md`** - Полная документация проекта
3. **`BACKEND_SUMMARY.md`** - Что было добавлено в бэкэнд
4. **`server/README.md`** - Подробная документация API
5. **`server/API_EXAMPLES.md`** - Примеры API запросов (curl, Postman)

---

## 🛑 Остановка приложения

Нажмите **Ctrl + C** в каждом терминале (или закройте терминал).

---

## 🆘 Часто задаваемые вопросы

### ❓ Почему сервер не запускается?

**Вероятная причина:** Порт 5000 уже занят

**Решение:**
1. Откройте `server/.env`
2. Измените строку `PORT=5000` на `PORT=5001` (или другой свободный порт)
3. Откройте `.env` в корне проекта
4. Измените `REACT_APP_API_URL=http://localhost:5000/api` на `REACT_APP_API_URL=http://localhost:5001/api`
5. Перезапустите приложение

### ❓ Приложение загружается но данные не видны?

**Вероятная причина:** Бэкэнд не запущен

**Решение:**
1. Откройте отдельный терминал
2. Выполните `npm run server:dev`
3. Убедитесь что вы видите сообщение `✅ Сервер запущен на порту 5000`
4. Обновите страницу в браузере (F5)

### ❓ Как протестировать API вручную?

Используйте **Postman** или **curl**:

```bash
# Получить все записи
curl http://localhost:5000/api/records

# Создать новую запись
curl -X POST http://localhost:5000/api/records \
  -H "Content-Type: application/json" \
  -d '{"client":"Test","car":"TestCar","service":"TestService","price":5000,"date":"2025-11-28","status":"pending"}'
```

Подробнее см. в `server/API_EXAMPLES.md`

### ❓ Где хранятся данные?

Все данные сохраняются в файл `server/data.json`. При первом запуске он создается автоматически с начальными данными.

### ❓ Можно ли использовать другую БД вместо JSON файла?

Да! Это возможно, но требует изменения кода. Полезные технологии:
- MongoDB
- PostgreSQL
- MySQL

Подробнее о возможных улучшениях см. в `BACKEND_SUMMARY.md`

---

## ⚡ Краткая справка команд

```bash
# Основные команды
npm run dev              # Запуск всего (фронтенд + бэкэнд) с автопересчитыванием
npm start                # Только фронтенд
npm run server:dev       # Только бэкэнд с автопересчитыванием
npm run server           # Только бэкэнд (production)
npm run build            # Сборка фронтенда для production
npm run start:both       # Запуск обоих (production)
```

---

## ✨ Отлично!

Вы готовы начать разработку! 🎊

**Теперь запустите:**
```bash
npm run dev
```

И откройте http://localhost:3000 в браузере!

---

**Вопросы?** Смотрите полную документацию в `README.md` или `server/README.md`

# 📚 Полная документация новых функций

## ✅ Реализованные функции

### 1️⃣ Управление услугами
**Файлы:** `server/routes/services.js`, `src/components/ServicesPage.js`

#### API Endpoints:
- `GET /api/services` - Получить все услуги с привязанными мастерами
- `GET /api/services/:id` - Получить услугу по ID
- `POST /api/services` - Создать новую услугу
- `PUT /api/services/:id` - Обновить услугу
- `DELETE /api/services/:id` - Удалить услугу

#### Пример создания услуги:
```bash
curl -X POST http://localhost:5000/api/services \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Замена масла",
    "price": 3500,
    "description": "Замена масла и масляного фильтра"
  }'
```

---

### 2️⃣ Управление пользователями
**Файлы:** `server/routes/users.js`, `src/components/UserRegistration.js`

#### Роли:
- **operator** - Оператор (может открывать/закрывать смены)
- **master** - Мастер (привязан к услугам)
- **client** - Клиент (регистрируется по номеру телефона)

#### API Endpoints:
- `GET /api/users?role=master` - Получить всех пользователей (можно фильтровать по роли)
- `GET /api/users/:id` - Получить пользователя по ID (с услугами/смены)
- `POST /api/users/register` - Регистрация клиента (только телефон и ФИО)
- `POST /api/users` - Создать пользователя (админ добавляет операторов/мастеров)
- `PUT /api/users/:id` - Обновить пользователя
- `DELETE /api/users/:id` - Удалить пользователя

#### Привязка услуг к мастерам:
- `POST /api/users/:masterId/services/:serviceId` - Привязать услугу к мастеру
- `DELETE /api/users/:masterId/services/:serviceId` - Отвязать услугу от мастера

#### Пример регистрации:
```bash
curl -X POST http://localhost:5000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+7 (999) 123-45-67",
    "full_name": "Иван Петров Сергеевич"
  }'
```

#### Пример создания оператора:
```bash
curl -X POST http://localhost:5000/api/users \
  -H "Content-Type: application/json" \
  -d '{
    "phone": "+7 (999) 111-11-11",
    "full_name": "Алексей Смирнов",
    "role": "operator"
  }'
```

---

### 3️⃣ Управление сменами
**Файлы:** `server/routes/shifts.js`, `src/components/ShiftManagement.js`

#### API Endpoints:
- `GET /api/shifts?operator_id=1&status=open` - Получить смены (с фильтрами)
- `GET /api/shifts/:id` - Получить смену по ID (с транзакциями)
- `POST /api/shifts/open/:operatorId` - Открыть смену
- `POST /api/shifts/close/:shiftId` - Закрыть смену
- `GET /api/shifts/operator/:operatorId/active` - Получить активную смену оператора
- `GET /api/shifts/:shiftId/logs` - Получить логи смены
- `GET /api/shifts/logs/operator/:operatorId` - Получить все логи оператора

#### Таблица логирования:
Все открытия и закрытия смен логируются в таблице `shift_logs` с:
- ID смены
- ID оператора
- Действие (opened/closed)
- Временная метка
- Дополнительные детали

#### Пример открытия смены:
```bash
curl -X POST http://localhost:5000/api/shifts/open/1 \
  -H "Content-Type: application/json"
```

---

### 4️⃣ Управление операциями платежей
**Файлы:** `server/routes/transactions.js`

#### API Endpoints:
- `GET /api/transactions?shift_id=1&operator_id=1` - Получить транзакции
- `GET /api/transactions/:id` - Получить транзакцию по ID
- `POST /api/transactions/payment` - Создать платеж
- `POST /api/transactions/cancellation` - Отменить платеж
- `GET /api/transactions/shift/:shiftId/total` - Получить итоги смены
- `GET /api/transactions/operator/:operatorId/report?start_date=...&end_date=...` - Отчет оператора

#### Условие: Платежи можно создавать только в открытой смене!

#### Пример создания платежа:
```bash
curl -X POST http://localhost:5000/api/transactions/payment \
  -H "Content-Type: application/json" \
  -d '{
    "shift_id": 1,
    "operator_id": 1,
    "amount": 3500,
    "description": "Оплата за замену масла"
  }'
```

#### Пример отмены платежа:
```bash
curl -X POST http://localhost:5000/api/transactions/cancellation \
  -H "Content-Type: application/json" \
  -d '{
    "transaction_id": 5,
    "reason": "Ошибка при вводе суммы"
  }'
```

---

### 5️⃣ Страница просмотра услуг
**Компонент:** `src/components/ServicesPage.js`

Функции:
- Отображение всех услуг в виде карточек
- Клик на услугу открывает модальное окно
- В модале видны все мастера, привязанные к этой услуге
- Отображение цены услуги и описания

---

### 6️⃣ Страница регистрации пользователей
**Компонент:** `src/components/UserRegistration.js`

Функции:
- Регистрация клиентов по номеру телефона и ФИО
- Валидация входных данных
- Проверка на уникальность номера телефона
- Дополнительная информация вносится администратором

---

### 7️⃣ Управление сменами оператора
**Компонент:** `src/components/ShiftManagement.js`

Функции:
- Открытие смены
- Закрытие смены
- Просмотр истории открытий/закрытий
- ❗️ После закрытия смены оператор не может выполнять никакие операции
- Все операции логируются

#### Правило: После закрытия смены ничего нельзя сделать оператору, пока он снова не откроет смену!

---

### 8️⃣ Логирование смен
**Таблица в БД:** `shift_logs`

Содержит:
- ID смены
- ID оператора
- Действие (opened/closed)
- Временная метка
- Детали операции

Пример данных в логе:
```json
{
  "id": 1,
  "shift_id": 5,
  "operator_id": 2,
  "action": "opened",
  "timestamp": "2025-03-15 09:30:00",
  "details": "Смена открыта оператором Алексей Смирнов"
}
```

---

## 📊 Структура базы данных

### Таблица: services
```sql
- id (PRIMARY KEY)
- name (VARCHAR, UNIQUE)
- price (DECIMAL)
- description (TEXT)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

### Таблица: users
```sql
- id (PRIMARY KEY)
- phone (VARCHAR, UNIQUE)
- full_name (VARCHAR)
- role (VARCHAR: 'operator', 'master', 'client')
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

### Таблица: master_services
```sql
- id (PRIMARY KEY)
- master_id (FOREIGN KEY -> users)
- service_id (FOREIGN KEY -> services)
- created_at (TIMESTAMP)
- UNIQUE(master_id, service_id)
```

### Таблица: shifts
```sql
- id (PRIMARY KEY)
- operator_id (FOREIGN KEY -> users)
- opened_at (TIMESTAMP)
- closed_at (TIMESTAMP, NULL если смена открыта)
- status (VARCHAR: 'open', 'closed')
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

### Таблица: transactions
```sql
- id (PRIMARY KEY)
- shift_id (FOREIGN KEY -> shifts)
- operator_id (FOREIGN KEY -> users)
- amount (DECIMAL)
- transaction_type (VARCHAR: 'payment', 'cancellation')
- description (TEXT)
- related_transaction_id (FOREIGN KEY -> transactions)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

### Таблица: shift_logs
```sql
- id (PRIMARY KEY)
- shift_id (FOREIGN KEY -> shifts)
- operator_id (FOREIGN KEY -> users)
- action (VARCHAR: 'opened', 'closed')
- timestamp (TIMESTAMP)
- details (TEXT)
- created_at (TIMESTAMP)
```

---

## 🔐 Проверки и ограничения

1. **Только открытые смены** - платежи можно создавать только в открытой смене
2. **Уникальность номера телефона** - невозможно зарегистрировать два пользователя с одинаковым номером
3. **Одна активная смена на оператора** - оператор не может открыть две смены одновременно
4. **Отмена только своих платежей** - платеж можно отменить только в той же открытой смене
5. **Логирование всех событий** - все открытия и закрытия смен записываются в логи

---

## 🚀 Как использовать

### Запуск проекта:
```bash
# Установка зависимостей
npm install
cd server && npm install && cd ..

# Запуск в режиме разработки
npm run dev

# Или отдельно
npm start          # React фронтенд на порту 3000
npm run server:dev # Node.js бэкенд на порту 5000
```

### Навигация в приложении:
- 📋 **Записи** - Основной список записей
- ➕ **Создать запись** - Форма создания новой записи
- 📝 **Регистрация** - Регистрация новых клиентов
- 📦 **Услуги** - Каталог услуг с привязанными мастерами
- 🔄 **Смены** - Управление сменами оператора

---

## 📝 Примеры использования React компонентов

### UserRegistration
```jsx
import UserRegistration from './components/UserRegistration';

<UserRegistration />
```

### ServicesPage
```jsx
import ServicesPage from './components/ServicesPage';

<ServicesPage />
```

### ShiftManagement
```jsx
import ShiftManagement from './components/ShiftManagement';

<ShiftManagement />
```

---

## 🐛 Обработка ошибок

Все API endpoints возвращают:
- `200` - Успешно
- `201` - Создано
- `400` - Ошибка клиента (неправильные данные)
- `404` - Не найдено
- `500` - Ошибка сервера

Пример ошибки:
```json
{
  "error": "Shift is not open",
  "details": "..."
}
```

---

## ✨ Особенности реализации

✅ Все смены содержат логи открытия/закрытия
✅ После закрытия смены оператор заблокирован
✅ Платежи привязаны к сменам
✅ Отмены платежей сохраняют связь с оригинальной операцией
✅ Регистрация клиентов простая (только телефон + ФИО)
✅ Администратор может добавлять операторов и мастеров
✅ Мастеры привязаны к услугам
✅ Транзакции в базе данных для целостности

---

## 📞 Поддержка

Для вопросов обратитесь к документации API в файле `/server/API_EXAMPLES.md`
